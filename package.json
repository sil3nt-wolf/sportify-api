{
  "name": "sportify-api",
  "version": "1.0.0",
  "description": "Free Spotify Search API by CASPER TECH — anonymous TOTP tokens, no credentials required",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js",
    "refresh": "node scripts/refresh.js"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "node-cron": "^3.0.3"
  }
}
